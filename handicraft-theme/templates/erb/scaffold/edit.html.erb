<%% title :model => <%= class_name %>.model_name.human, :name => @<%= singular_name %>.<%= attributes.first.name %> %>

<%%= render 'form' %>

<%%= link_to t('links.show'), @<%= singular_name %> %> |
<%%= link_to t('links.index'), <%= plural_name %>_path %>
